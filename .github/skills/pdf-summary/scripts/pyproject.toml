[project]
name = "pdf-summary"
version = "0.1.0"
description = "Extract and summarize CAN protocol PDF documents with Typer CLI"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pdfplumber>=0.10.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
]

[tool.uv]
python-version = "3.11"
